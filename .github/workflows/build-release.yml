name: Build and Release APK

on:
  push:
    branches: [ debug-fix, main ]
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'
      
      - name: Verify Flutter
        run: |
          flutter --version
          flutter doctor -v
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build APK
        run: flutter build apk --release --no-shrink
        timeout-minutes: 15
      
      - name: Verify APK exists
        run: |
          ls -la build/app/outputs/flutter-apk/
          file build/app/outputs/flutter-apk/app-release.apk
          stat build/app/outputs/flutter-apk/app-release.apk
      
      - name: Upload APK to Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/debug-fix' || github.ref == 'refs/heads/main'
        with:
          files: build/app/outputs/flutter-apk/app-release.apk
          tag_name: v1.0.0-build-${{ github.run_number }}
          name: NieuwsApp Build ${{ github.run_number }}
          body: |
            Automated build from ${{ github.ref_name }} branch
            - Flutter 3.24.5
            - Proguard fixes applied
            - Build date: ${{ github.event.head_commit.timestamp }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}