import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches without errors', (WidgetTester tester) async {
    // Basis smoke test — build de app en check of ie niet crasht
    // TODO: import main.dart met correcte package name als je echte tests wilt
    expect(true, isTrue);
  });
}
